---
phase: 03-user-management
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - Frontend/src/Pages/AdminUsers.jsx
autonomous: true

must_haves:
  truths:
    - "Admin sees a paginated user list with status and signup date"
    - "Admin can search users by name or email and filter by signup date range"
    - "Each user row links to a detail view"
  artifacts:
    - path: "Frontend/src/Pages/AdminUsers.jsx"
      provides: "User list UI with filters, pagination, and status badges"
      min_lines: 80
  key_links:
    - from: "Frontend/src/Pages/AdminUsers.jsx"
      to: "/admin/users"
      via: "fetch with Clerk token"
      pattern: "admin/users"
    - from: "Frontend/src/Pages/AdminUsers.jsx"
      to: "/admin/users/:userId"
      via: "Link"
      pattern: "\/admin\/users\/"
---

<objective>
Build the admin user list UI with search, date filters, status badges, and pagination.

Purpose: Let admins quickly find users and navigate into detail views.
Output: User list page wired to admin user list API.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Frontend/src/Pages/AdminUsers.jsx
@Frontend/src/Pages/AdminDashboard.jsx
@Frontend/src/Entities/api.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement user list data fetching with filters</name>
  <files>Frontend/src/Pages/AdminUsers.jsx</files>
  <action>
Add Clerk-authenticated fetch logic using `useAuth` + `API_URL` to call `/admin/users` with `search`, `start_date`, `end_date`, `limit`, and `offset` params.
Maintain local state for inputs vs applied filters, resetting pagination on apply.
Handle loading, error, and empty states using the same fetch pattern as AdminDashboard.
  </action>
  <verify>Open /admin/users, apply search/date filters, and confirm requests include query params.</verify>
  <done>Filters update the list and pagination resets when filters change.</done>
</task>

<task type="auto">
  <name>Task 2: Render table, status badges, and pagination controls</name>
  <files>Frontend/src/Pages/AdminUsers.jsx</files>
  <action>
Replace the placeholder with a table layout that shows user name, email, signup date, and status (active/suspended).
Add a per-row action link to `/admin/users/{id}` and show total count with previous/next pagination controls.
Use shadcn/ui `Button` and `Input` styles, matching the admin dashboard visual language.
  </action>
  <verify>List shows status badges and the detail link navigates to `/admin/users/{id}`.</verify>
  <done>User list displays paginated rows with status and navigation into details.</done>
</task>

</tasks>

<verification>
- Visit `/admin/users` and confirm the list loads with status badges.
- Search by email substring and verify the result count changes.
- Set a date range and confirm the list updates and pagination works.
</verification>

<success_criteria>
Admins can search and filter users, see status and signup dates, and navigate to detail views.
</success_criteria>

<output>
After completion, create `.planning/phases/03-user-management/03-02-SUMMARY.md`
</output>
