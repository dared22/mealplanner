---
phase: 05-activity-logging
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - Frontend/src/Pages/AdminLogs.jsx
autonomous: true
must_haves:
  truths:
    - "Admin can view a paginated activity log table with timestamps, action details, actor, and status."
    - "Admin can filter activity logs by date range, actor type, and status."
  artifacts:
    - path: "Frontend/src/Pages/AdminLogs.jsx"
      provides: "Activity logs UI"
      contains: "Activity Logs"
  key_links:
    - from: "Frontend/src/Pages/AdminLogs.jsx"
      to: "/admin/logs"
      via: "fetch"
      pattern: "/admin/logs"
---

<objective>
Build the Activity Logs screen to display and filter log entries.

Purpose: Give administrators a clear audit trail with filtering and pagination controls.
Output: Activity Logs page wired to the /admin/logs API.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Frontend/src/Pages/AdminLogs.jsx
@Frontend/src/Pages/AdminUsers.jsx
@Frontend/src/Pages/AdminRecipes.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add filters and data fetching</name>
  <files>Frontend/src/Pages/AdminLogs.jsx</files>
  <action>
Replace the placeholder with a full Activity Logs page. Add state for status, error, logs, pagination, and filter inputs (date range, actor type, status). Implement fetchLogs using Clerk getToken and API_URL, mirroring AdminUsers/AdminRecipes patterns (apply/clear filters, URLSearchParams, loading/error states).
  </action>
  <verify>npm run lint</verify>
  <done>Activity logs are fetched from /admin/logs with filter and pagination parameters.</done>
</task>

<task type="auto">
  <name>Task 2: Render table, filters, and pagination</name>
  <files>Frontend/src/Pages/AdminLogs.jsx</files>
  <action>
Render filter controls (inputs/selects + Apply/Clear buttons), table columns (timestamp, action, actor, status), status badges, empty state, and pagination controls consistent with the AdminUsers/AdminRecipes UI styling. Include retry UI for error state and loading spinner.
  </action>
  <verify>npm run lint</verify>
  <done>Admin can browse logs, filter them, and paginate through results in the Activity Logs UI.</done>
</task>

</tasks>

<verification>
Activity Logs page renders data from /admin/logs and supports filter and pagination flows.
</verification>

<success_criteria>
Admin UI provides a functional activity log table aligned with Phase 5 requirements.
</success_criteria>

<output>
After completion, create `.planning/phases/05-activity-logging/05-03-SUMMARY.md`
</output>
